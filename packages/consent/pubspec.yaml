name: genesis_consent
description: >-
  An enforce/reject action substrate: validate A2UI actions by hit-testing the
  live tree against catalog-declared affordances, then enforce or refuse.
version: 0.2.0-dev.1
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  genesis_dialogue: ^0.2.0-dev.1
  genesis_taxonomy: ^0.2.0-dev.1
  genesis_tree: ^0.5.0-dev.1
dev_dependencies:
  build_runner: ^2.15.0
  genesis_perception: ^0.4.0-dev.1
  lints: ^5.1.0
  test: ^1.25.0
