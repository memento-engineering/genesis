name: genesis_dialogue
description: >-
  The A2UI v0.9 wire format: parse and serialize the updateComponents envelope
  and reconcile re-emissions by key onto a genesis_tree.
version: 0.1.1
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  genesis_taxonomy: ^0.1.0
  genesis_tree: ^0.1.0
  meta: ^1.15.0

dev_dependencies:
  build_runner: ^2.15.0
  genesis_perception: ^0.1.0
  lints: ^5.1.0
  test: ^1.25.0
