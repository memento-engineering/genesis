name: genesis_perception
description: >-
  A measurement domain built on the genesis_tree spine: read-only perception
  trees with Node/Field leaves and a harvest pipeline.
version: 0.1.0
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  genesis_tree: ^0.1.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
