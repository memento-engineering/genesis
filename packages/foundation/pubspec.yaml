name: genesis_foundation
description: Dependency-free diagnostics protocols and typed, versioned tree snapshots.
version: 0.1.1
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
