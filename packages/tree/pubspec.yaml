name: genesis_tree
description: >-
  A framework-agnostic Seed/Branch keyed-reconcile tree engine — Flutter's
  element/reconciliation model extracted to pure, bare-VM Dart.
version: 0.1.2
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
