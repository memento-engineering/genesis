name: genesis_typesetting
description: >-
  The cell/terminal render backend: a double-buffered character grid set in
  minimal ANSI, repainted from the tree's drained dirty set (ADR-0004).
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  genesis_tree: any
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
