name: genesis_taxonomy
description: >-
  Schema-first UI catalog codegen: one catalog generates a typed Dart component
  registry and a matching LLM tool schema.
version: 0.1.0
repository: https://github.com/memento-engineering/genesis
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  build: ^4.0.6
  dart_style: ^3.1.9
  genesis_tree: ^0.1.0
  meta: ^1.15.0

dev_dependencies:
  build_runner: ^2.15.0
  json_schema: ^5.2.2
  lints: ^5.1.0
  test: ^1.25.0
